// make a page that shows total price and only cash on delievery option
// make a button that says "Place Order" and when clicked, it will send a post request to the backend

import React from 'react'

const Payment = () => {
  return (
    <div>

    </div>
  )
}

export default Payment